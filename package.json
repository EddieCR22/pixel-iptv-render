{
  "name": "pixel-iptv-render-proxy",
  "version": "5.0.0",
  "private": true,
  "description": "Proxy HTTPS seguro y robusto para Pixel IPTV",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
