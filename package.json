{
  "name": "pixel-iptv-render-proxy",
  "version": "11.0.0",
  "private": true,
  "description": "Proxy HTTPS estable para Pixel IPTV con audio AAC TS-first y fallback HLS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0"
  }
}
